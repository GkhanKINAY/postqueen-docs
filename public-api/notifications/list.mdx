---
title: "List notifications"
sidebarTitle: "List notifications"
description: "The workspace's notifications, such as published and failed posts, newest first."
openapi: "GET /notifications"
---

These are the notifications the app shows. Page through them with `page`, starting at `0`, until `hasMore` is `false`. To be told the moment a post goes out instead of polling, use a [webhook](/using/webhooks).
