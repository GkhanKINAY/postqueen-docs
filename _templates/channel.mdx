---
title: "NETWORK"
description: "Connect NETWORK to PostQueen, what you can post to it, and the limits PostQueen checks before a post is scheduled."
icon: "/images/channels/SLUG.svg"
---

import { ChannelStatus } from '/snippets/status-badge.mdx';
import NextStepsChannel from '/snippets/next-steps-channel.mdx';

{/*
  Channel page template (B2 to B5). Copy it to channels/<slug>.mdx and fill every
  UPPER_CASE word. The facts come from facts/facts.json and the network's entry in
  facts_channels (research folder); every label, limit and message is re-checked
  against the app's origin/main. Delete these comments when the page is done.

  Rules for this page:
  - No self-hosting, no env var names, no source links, never "your system" or "she".
  - Examples and wording from facts_channels `safeToPublish`; nothing from `doNotSay`.
  - The never-share snippet only on networks that take a key or password
    (Bluesky, Nostr, Lemmy, Listmonk, WordPress, DEV, Hashnode). Not Moltbook:
    PostQueen registers its agent, so nobody types a key.
  - Status Soon (no_keys, cannot_connect): keep "What you can post", "Post
    settings" and "Good to know", replace "Connect" with the Soon note, and drop
    the connect steps and troubleshooting.
  - Add `tag: "Soon"` to the frontmatter of a Soon network.
*/}

<ChannelStatus status="STATUS">STATUS_NOTE_FROM_FACTS_JSON</ChannelStatus>

{/* One or two sentences: what NETWORK is for and what PostQueen posts to it. */}

ONE_LINER

## What you can post

<CardGroup cols={2}>
  <Card title="Text" icon="type">
    TEXT_LIMIT, counted the way NETWORK counts it (characters or bytes).
  </Card>
  <Card title="Media" icon="image">
    MEDIA_RULE: how many images or videos, and which formats.
  </Card>
  <Card title="Post types" icon="layers">
    POST_TYPES: posts, carousels, stories, reels, threads.
  </Card>
  <Card title="Analytics" icon="chart-line">
    ANALYTICS_LINE, or "No analytics for NETWORK."
  </Card>
</CardGroup>

**Not supported:** UNSUPPORTED_POST_TYPES.

## Checked before scheduling

PostQueen refuses a post that breaks these rules and shows the message in the composer:

| Rule | Message |
|---|---|
| RULE | "EXACT MESSAGE FROM THE APP" |

RULES_NOT_PRECHECKED: the platform rules PostQueen does not check first, in one sentence.

## Connect NETWORK

{/* For a Soon network, replace this section with:
<Note>
NETWORK is listed in **Add Channel**, but it cannot be connected on PostQueen yet.
</Note>
*/}

<Steps>
  <Step title="Open Add Channel">
    Open **Channels** and choose **Add Channel**, then **TILE_NAME**.
  </Step>
  <Step title="Sign in to NETWORK">
    CONNECT_STEP
  </Step>
  <Step title="Check that it connected">
    The channel appears in the **Channels** list with its avatar and name.
  </Step>
</Steps>

## Post settings

SETTINGS_IN_PLAIN_ENGLISH. The field names and values for the API are on [NETWORK posting settings](/public-api/providers/API_SLUG).

## Comments and threads

FOLLOW_UPS: what **Add comment** or **Continue thread** does on NETWORK, the delay, and whether media is allowed.

## Good to know

- CAVEAT

## Troubleshooting

<AccordionGroup>
  <Accordion title="EXACT ERROR MESSAGE">
    What it means and what to do.
  </Accordion>
</AccordionGroup>

<NextStepsChannel />
