---
title: Client Setup
description: 'Configure your AI client to connect to the PostQueen MCP server'
---

## Claude Desktop

Add the following to your Claude Desktop MCP configuration file:

<Tabs>
  <Tab title="macOS">
    Edit `~/Library/Application Support/Claude/claude_desktop_config.json`:

    ```json
    {
      "mcpServers": {
        "postqueen": {
          "url": "https://api.postqueen.ai/mcp/your-api-key"
        }
      }
    }
    ```
  </Tab>
  <Tab title="Windows">
    Edit `%APPDATA%\Claude\claude_desktop_config.json`:

    ```json
    {
      "mcpServers": {
        "postqueen": {
          "url": "https://api.postqueen.ai/mcp/your-api-key"
        }
      }
    }
    ```
  </Tab>
</Tabs>

Replace `your-api-key` with your actual API key from **Settings > Developers > Public API**.

## Claude Code

The fastest way is the CLI:

```bash
claude mcp add postqueen --transport http --url https://api.postqueen.ai/mcp/your-api-key
```

Or add it to your Claude Code config directly:

```json
{
  "mcpServers": {
    "postqueen": {
      "url": "https://api.postqueen.ai/mcp/your-api-key"
    }
  }
}
```

The Bearer-token transport works too — pick whichever your client supports better. Both authenticate the same way:

```bash
claude mcp add postqueen --transport http \
  --url https://api.postqueen.ai/mcp \
  --header "Authorization: Bearer your-api-key"
```

## Cursor

In Cursor, go to **Settings > MCP** and add a new server:

- **Name:** PostQueen
- **Type:** HTTP
- **URL:** `https://api.postqueen.ai/mcp/your-api-key`

## Other MCP Clients

Any MCP-compatible client can connect to PostQueen. Use the streamable HTTP transport:

- **URL:** `https://api.postqueen.ai/mcp/your-api-key`
- **Transport:** Streamable HTTP

Or, if your client supports Bearer token authentication:

- **URL:** `https://api.postqueen.ai/mcp`
- **Transport:** Streamable HTTP
- **Authorization:** `Bearer your-api-key`

## Self-Hosted

For self-hosted PostQueen instances, replace `https://api.postqueen.ai` with your `NEXT_PUBLIC_BACKEND_URL`:

```
https://your-postqueen-server.com/mcp/your-api-key
```

## Verify Connection

Once connected, ask your AI agent:

> "List my connected social media accounts"

If the connection is working, the agent will call the `integrationList` tool and return your connected accounts.
