---
title: Client Setup
description: 'Connect your AI client to the PostQueen MCP server'
---

PostQueen speaks MCP over streamable HTTP, so any MCP-compatible client can connect to her with a single URL. Pick your client from the grid below for a step-by-step guide, or use the endpoints directly if you already know your way around MCP configs.

## Endpoints

The server accepts two connection forms. Both authenticate with the same API key.

<Tabs>
  <Tab title="API Key in URL">
    ```
    https://api.postqueen.ai/mcp/YOUR_API_KEY
    ```
  </Tab>
  <Tab title="Bearer Token">
    ```
    URL: https://api.postqueen.ai/mcp
    Authorization: Bearer YOUR_API_KEY
    ```
  </Tab>
</Tabs>

Your API key lives in the PostQueen app at [app.postqueen.ai](https://app.postqueen.ai): open **Settings**, then **Developers**, then **Public API**, and click **Reveal**.

<Warning>
The connector URL contains your API key, and that key can post to every channel you have connected. Treat it like a password. If the key leaks, regenerate it in **Settings**, then **Developers**, then **Public API**, and update every client that uses it.
</Warning>

## Pick Your Client

<CardGroup cols={2}>
  <Card title="Claude Code" icon="terminal" href="/mcp/clients/claude-code">
    Add PostQueen with one `claude mcp add` command
  </Card>
  <Card title="Claude Desktop" icon="desktop" href="/mcp/clients/claude-desktop">
    Connect through the desktop app on macOS or Windows
  </Card>
  <Card title="Claude Web & Mobile" icon="mobile" href="/mcp/clients/claude-app">
    Add PostQueen as a connector on claude.ai
  </Card>
  <Card title="ChatGPT" icon="message" href="/mcp/clients/chatgpt">
    Use PostQueen from ChatGPT connectors
  </Card>
  <Card title="Cursor" icon="arrow-pointer" href="/mcp/clients/cursor">
    Configure the MCP server in Cursor settings
  </Card>
  <Card title="Codex" icon="code" href="/mcp/clients/codex">
    Wire PostQueen into the Codex CLI
  </Card>
  <Card title="Gemini CLI" icon="wand-magic-sparkles" href="/mcp/clients/gemini-cli">
    Register the server in your Gemini CLI settings
  </Card>
  <Card title="Other Clients" icon="puzzle-piece" href="/mcp/clients/other-clients">
    Generic setup for any MCP-compatible client
  </Card>
</CardGroup>

## Self-Hosted

On a self-hosted instance, replace `https://api.postqueen.ai` with your `NEXT_PUBLIC_BACKEND_URL`:

```
https://your-postqueen-server.com/api/mcp/YOUR_API_KEY
```

Most self-hosted setups serve the backend under `/api`, so the MCP path sits at `/api/mcp`. Use whatever `NEXT_PUBLIC_BACKEND_URL` is set to, then append `/mcp/YOUR_API_KEY`.

Your reverse proxy must forward the `/mcp` paths to the backend and support streaming HTTP. See [Reverse Proxies](/reverse-proxies/caddy).

## Verify

Once connected, ask your AI agent:

> "List my connected social media accounts"

If the connection works, the agent calls the `integrationList` tool and returns your connected accounts. If it fails, check that the API key was pasted in full and that your client uses the streamable HTTP transport.
