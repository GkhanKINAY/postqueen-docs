---
title: Muse Code
description: "Connect Muse Code, Meta's coding agent, to PostQueen over streamable HTTP."
icon: /images/brand/icon-muse.svg
---

{/* OVERHAUL-TODO(B7): write it from _templates/agent.mdx. Sources: this file, moved from mcp/clients/muse (merge; WIP: partly better; audit: agents). Row in page-status-2026-09-23.md. */}

**Muse Code** is Meta's coding agent for the terminal and CI. It loads remote MCP servers from `~/.config/muse/settings.json` over streamable HTTP.

<Note>
From Meta's docs, not tested by PostQueen yet.
</Note>

Looking for **Muse**, Meta's personal agent in the Muse app and WhatsApp? It is a different agent and connects a different way: [Muse, the personal agent](/agents/muse#muse-the-personal-agent).

<Info>
You need your PostQueen API key first. In [app.postqueen.ai](https://app.postqueen.ai), open **Connections > API Keys** and click **Reveal**. Only a workspace admin can reveal the key.
</Info>

## Muse Code

<Steps>
  <Step title="Edit Muse Code settings">
    Add this to `~/.config/muse/settings.json`. The file must contain `"schema_version": 1`, and `"mode": "optional"` keeps a failing server from stopping the run. Restart Muse Code after saving.

    ```json
    {
      "schema_version": 1,
      "mcp_servers": {
        "postqueen": {
          "transport": "streamable_http",
          "url": "https://api.postqueen.ai/mcp/YOUR_API_KEY",
          "mode": "optional"
        }
      }
    }
    ```
  </Step>
  <Step title="Or pass the key as a header">
    If you prefer a bare `/mcp` URL, put the key in `Authorization`:

    ```json
    {
      "schema_version": 1,
      "mcp_servers": {
        "postqueen": {
          "transport": "streamable_http",
          "url": "https://api.postqueen.ai/mcp",
          "headers": {
            "Authorization": "Bearer YOUR_API_KEY"
          },
          "mode": "optional"
        }
      }
    }
    ```
  </Step>
  <Step title="Confirm">
    Type `/mcp` to see the PostQueen tools, then ask Muse Code to list your PostQueen channels. Your account names come back when it calls `integrationList`.
  </Step>
</Steps>

<Warning>
Your API key grants full access to your PostQueen workspace: treat it like a password. Keep `~/.config/muse/settings.json` private. If the key leaks, have an admin rotate it under **Connections > API Keys**, then update the file.
</Warning>

## Self-hosted

On a self-hosted instance, replace `https://api.postqueen.ai` with your `NEXT_PUBLIC_BACKEND_URL`, for example `https://postqueen.example.com/api/mcp/YOUR_API_KEY`. Your reverse proxy has to forward `/mcp` and stream the response: [MCP on a self-hosted install](/configuration/mcp).

## Next steps

<CardGroup cols={2}>
  <Card title="Muse Code and Muse" icon="/images/brand/icon-muse.svg" href="/agents/muse">
    Muse Code at a glance, and how Muse, the personal agent, connects
  </Card>
  <Card title="MCP tools" icon="wrench" href="/mcp/tools">
    All 21 tools Muse Code discovers on connect
  </Card>
</CardGroup>
